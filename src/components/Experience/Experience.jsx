// When using JSX, React must be in scope
import React from 'react';
import './Experience.css';

const NewExperience = ({ experience}) => {
  if (user.profile)
  let experience = {
    user= user.req.id,
    profile = user,
    job = jobTitle,
    acivity = acivity
  }
  return (
    <div className='Experience'>
      {NewExperience.map((experience, idx) =>
        <div
          experience
        />
      )}
    </div>
  );
}

export default NewExperience;